<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/posts/16107.html</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>逆向CTF系列1-Hello，CTF</title>
    <url>/posts/1.html</url>
    <content><![CDATA[<h1 id="逆向CTF系列1-Hello，CTF"><a href="#逆向CTF系列1-Hello，CTF" class="headerlink" title="逆向CTF系列1-Hello，CTF"></a>逆向CTF系列1-Hello，CTF</h1><p>下载文件首先查壳</p>
<p><img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20241011170415935.png" alt="image-20241011170415935"></p>
<p>是32位的。</p>
<p>上面的图看到了没有。说明这个软件是没有加壳的。是用Microsoft Visual C++ 6.0软件编写的。如果查到了是aspack的。那就说明是加了aspack的壳。您可以用专门脱aspack脱壳机来脱壳。很多高手都是用OllyDBG V1.10 手工来脱壳的。</p>
<p>可以先运行一下源程序，输入123，发现输出wrong</p>
<p><img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20241011213951422.png" alt="image-20241011213951422"></p>
<p>可能是要我们输出success才行。在ida中再看看，果断F5。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> __cdecl <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> **argv, <span class="type">const</span> <span class="type">char</span> **envp)</span> &#123;</span><br><span class="line">  <span class="type">int</span> i; <span class="comment">// ebx</span></span><br><span class="line">  <span class="type">char</span> v4; <span class="comment">// al</span></span><br><span class="line">  <span class="type">int</span> result; <span class="comment">// eax</span></span><br><span class="line">  <span class="type">int</span> v6; <span class="comment">// [esp+0h] [ebp-70h]</span></span><br><span class="line">  <span class="type">int</span> v7; <span class="comment">// [esp+0h] [ebp-70h]</span></span><br><span class="line">  <span class="type">char</span> Buffer[<span class="number">2</span>]; <span class="comment">// [esp+12h] [ebp-5Eh] BYREF</span></span><br><span class="line">  <span class="type">char</span> v9[<span class="number">20</span>]; <span class="comment">// [esp+14h] [ebp-5Ch] BYREF</span></span><br><span class="line">  <span class="type">char</span> v10[<span class="number">32</span>]; <span class="comment">// [esp+28h] [ebp-48h] BYREF</span></span><br><span class="line">  __int16 v11; <span class="comment">// [esp+48h] [ebp-28h]</span></span><br><span class="line">  <span class="type">char</span> v12; <span class="comment">// [esp+4Ah] [ebp-26h]</span></span><br><span class="line">  <span class="type">char</span> v13[<span class="number">36</span>]; <span class="comment">// [esp+4Ch] [ebp-24h] BYREF</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">strcpy</span>(v13, <span class="string">&quot;437261636b4d654a757374466f7246756e&quot;</span>);</span><br><span class="line">  <span class="keyword">while</span> ( <span class="number">1</span> )&#123;</span><br><span class="line">    <span class="built_in">memset</span>(v10, <span class="number">0</span>, <span class="keyword">sizeof</span>(v10));</span><br><span class="line">    v11 = <span class="number">0</span>;</span><br><span class="line">    v12 = <span class="number">0</span>;</span><br><span class="line">    sub_40134B(aPleaseInputYou, v6);</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%s&quot;</span>, v9);</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strlen</span>(v9) &gt; <span class="number">0x11</span> ) <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">for</span> ( i = <span class="number">0</span>; i &lt; <span class="number">17</span>; ++i ) &#123;</span><br><span class="line">      v4 = v9[i];</span><br><span class="line">      <span class="keyword">if</span> ( !v4 ) <span class="keyword">break</span>;</span><br><span class="line">      <span class="built_in">sprintf</span>(Buffer, <span class="string">&quot;%x&quot;</span>, v4);</span><br><span class="line">      <span class="built_in">strcat</span>(v10, Buffer);</span><br><span class="line">    &#125;</span><br><span class="line">     <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(v10, v13))  sub_40134B(aSuccess, v7);</span><br><span class="line">     <span class="keyword">else</span> sub_40134B(aWrong, v7);</span><br><span class="line">  &#125;</span><br><span class="line">  sub_40134B(aWrong, v7);</span><br><span class="line">  result = --Stream._cnt;</span><br><span class="line">  <span class="keyword">if</span> ( Stream._cnt &lt; <span class="number">0</span> )</span><br><span class="line">    <span class="keyword">return</span> _filbuf(&amp;Stream);</span><br><span class="line">  ++Stream._ptr;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>sub_40134B没搜到他的用途，但是读完代码并理解后可以假设一下他是输出函数，也就是，在v10和v13相等才会输出v13</p>
<p>简单讲一下代码逻辑，输入v9，v9长度不能大于0x11(0x11&#x3D;17,16进制，因为0x)，循环17次，取出每一位如果为0则退出循环，不为0则拼接到v10(16进制)，循环结束后进行比较。</p>
<p>也就是437261636b4d654a757374466f7246756e是输入字符串的16进制</p>
<blockquote>
<p>％x以十六进制数形式输出整数</p>
</blockquote>
<p><img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20241011220112125.png" alt="image-20241011220112125"></p>
<p>当然也可以写脚本：</p>
<p><code>16</code>进制字符串转字符串，大致的逻辑就是将<code>16</code>进制字符串<strong>每两个字符</strong>组合在一起得到一个<code>16</code>进制的字符串</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">x=<span class="string">&#x27;437261636b4d654a757374466f7246756e&#x27;</span> </span><br><span class="line">y=<span class="string">&#x27;&#x27;</span> </span><br><span class="line">i=<span class="number">0</span></span><br><span class="line"><span class="keyword">while</span>(i &lt; <span class="built_in">len</span>(x)):</span><br><span class="line">    y += <span class="built_in">chr</span>(<span class="built_in">int</span>(x[i:i+<span class="number">2</span>],<span class="number">16</span>))</span><br><span class="line">    i += <span class="number">2</span></span><br><span class="line"><span class="built_in">print</span>(y)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>以下是 int() 方法的语法:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class int(x, base=10)</span><br></pre></td></tr></table></figure>

<p>参数</p>
<ul>
<li>x – 字符串或数字。</li>
<li>base – 进制数，默认十进制。</li>
</ul>
</blockquote>
<p>CrackMeJustForFun</p>
<p>成功，CrackMeJustForFun就是flag</p>
<p><img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20241011220412699.png" alt="image-20241011220412699"></p>
<p><strong>补充一下壳的知识：</strong></p>
<blockquote>
<p>壳是指在一个程序外面包裹一段代码，保护里面的代码不被正确反编译或非法修改，在加载到内存时般先于程序运行，拿到控制权，然后保护并加载程序。</p>
<p>加壳:其实是利用特殊的算法，对EXE、DLL文件里的资源进行压缩。类似WINZIP 的效果，只不过这个压缩之后的文件，可以独立运行，解压过程完全隐蔽，都在内存中完成。加壳过的程序可以直接运行但是不能查看源代码，要经过脱壳才可以查看源代码，这样可以起到保护程序的作用。</p>
</blockquote>
<p><img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20241011171006197.png" alt="image-20241011171006197"></p>
<ul>
<li>压缩壳:通过压缩算法压缩软件大小。</li>
<li>加密壳:利用各种反跟踪技术保护程序不被调试、脱壳、反编译。</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>计算机保研复习-数据结构</title>
    <url>//archives/048bdd49-43a9-4694-8f5b-126c986291dd</url>
    <content><![CDATA[<h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h1 id="一、线性表"><a href="#一、线性表" class="headerlink" title="一、线性表"></a>一、线性表</h1><h2 id="1-1-数组和链表的区别"><a href="#1-1-数组和链表的区别" class="headerlink" title="1.1 数组和链表的区别"></a>1.1 数组和链表的区别</h2><p><strong>线性表</strong>是具有相同数据类型的n个数据元素的有限序列</p>
<ul>
<li><p>物理<strong>存储结构</strong>不同：</p>
<ul>
<li><p>数组是顺序存储结构，存储长度是固定的，它不能适应数据动态增减的情况；</p>
</li>
<li><p>链表是链式存储结构，能够动态分配存储空间以适应数据动态增减的情况；</p>
</li>
</ul>
</li>
<li><p><strong>内存分配方式</strong>不同：</p>
<ul>
<li><p>数组的存储空间一般采用静态分配；</p>
</li>
<li><p>链表的存储空间一般采用动态分配；</p>
</li>
</ul>
</li>
<li><p>元素的<strong>存取方式</strong>不同：</p>
<ul>
<li><p>数组元素为直接存取；</p>
</li>
<li><p>链表元素的存取需要遍历链表；</p>
</li>
</ul>
</li>
<li><p>元素的<strong>插入和删除方式</strong>不同：</p>
<ul>
<li>数组进行元素插入和删除时，需要移动数组的元素；</li>
<li>链表进行元素插入和删除时，无需移动链表内的元素；</li>
</ul>
</li>
</ul>
<h2 id="1-2-链表种类"><a href="#1-2-链表种类" class="headerlink" title="1.2 链表种类"></a>1.2 链表种类</h2><ol>
<li>单链表</li>
<li>双链表</li>
<li>循环单链 双链表</li>
</ol>
<p>​	好处：单链表只能从表头结点开始遍历所有结点，而循环单链表可以从表中的任一结点开始遍历整个链表。对单链表我们常常设立头	指针，这样在表头表尾操作的时间复杂度分别为O(1)和O(n)，而对于循环列表常常设立尾指针，时间复杂度都为O(1)</p>
<ol start="4">
<li>静态链表：借助数组描述线性表的链式存储结构，需要预先分配一块连续的内存空间。</li>
</ol>
<p>​	优点：增删改查不用移动大量元素。</p>
<p>​	缺点：不能随机存取，容量固定不可变。(不支持指针的语言)</p>
<p>尾插法得遍历(维护长度)</p>
<p>头插法直接在前面插入</p>
<h2 id="1-3-栈与队列"><a href="#1-3-栈与队列" class="headerlink" title="&#x3D;&#x3D;1.3 栈与队列&#x3D;&#x3D;"></a>&#x3D;&#x3D;1.3 栈与队列&#x3D;&#x3D;</h2><p>栈：是只允许在一端进行插入或删除操作的线性表</p>
<p>队列：也是一种操作受限的线性表，只允许在表的一端进行插入，而在表的另一端进行删除</p>
<p><strong>栈</strong>：顺序栈利用一组地址连续的存储单元存放数据元素，同时附设一个指针指示当前栈顶元素位置。链栈是利用一个单链表存储数据元素，并将表头作为栈顶进行插入和删除操作。</p>
<p><strong>队列</strong>：顺序存储是利用一组地址连续的存储单元存放数据元素，并附设两个指针，通常front指针指向队头元素，rear指针指向队尾元素的下一个位置。链式存储是利用一个带有头指针和尾指针的链表存储数据元素。</p>
<h2 id="1-4-数据存储方式"><a href="#1-4-数据存储方式" class="headerlink" title="&#x3D;&#x3D;1.4 数据存储方式&#x3D;&#x3D;"></a>&#x3D;&#x3D;1.4 数据存储方式&#x3D;&#x3D;</h2><p><strong>顺序存储结构的优点：</strong></p>
<p><strong>顺序存储</strong>是指在内存中开辟连续的存储空间来存放数据，把逻辑上相邻的元素存储在物理位置也相邻的存储单元中。</p>
<ol>
<li><p>可以随机存取，即通过首地址和元素序号可在时间O(1)内找到指定的元素，链表只能从表头开始遍历，顺序存取元素。</p>
</li>
<li><p>存储密度高，每个节点只存储数据元素（链式存储还要存指针）</p>
<p><strong>缺点：</strong>只能使用相邻的一整块存储单元，因此可能产生较多的外部碎片。</p>
</li>
</ol>
<p><strong>链式存储结构的优点：</strong></p>
<p><strong>链式存储</strong>的存储空间不是连续的，借助指针来表示元素间的逻辑关系。还需要存放一个指向其后继的指针，指向下一元素。</p>
<ol>
<li><p>进行插入、删除操作时很方便，不需要移动数据元素。</p>
<p>顺序存储在进行插入操作时需要将插入位置之后的元素全部后移一位，时间复杂度为O(n)，而链式存储只需要修改插入位置前驱后继节点指针的指向，时间复杂度为o(1)；</p>
</li>
<li><p>不用预先估计存储空间的规模，在需要时向内存申请空间即可，操作灵活。</p>
<p>而顺序存储包含两种方式，静态分配和动态分配，若采用静态分配，一旦存储空间满了就不能再扩充，因此为了规避内存溢出的风险，只能尽量分配足够大的存储空间，但这样又会导致空间的浪费。而动态分配虽然可以扩充存储空间，但是需要移动大量元素，操作效率低。</p>
</li>
<li><p>不会出现碎片现象，能充分利用所有存储单元</p>
</li>
<li><p>每个元素因存储指针而占用额外的存储空间</p>
</li>
</ol>
<p><strong>索引存储</strong>：</p>
<ul>
<li><p>优点：检索速度快</p>
</li>
<li><p>缺点：附加的索引表额外占用存储空间，增加和删除元素时也要修改索引表。</p>
</li>
</ul>
<p><strong>散列存储</strong>：根据元素的关键字直接计算出该元素的存储地址。</p>
<p><strong>优点</strong>：检索、增加和删除结点的操作都很快；<strong>缺点</strong>是若散列函数不好，则可能出现元素存储单元冲突，而解决冲突会增加时间空间开销。</p>
<h2 id="1-5-头指针和头节点的区别"><a href="#1-5-头指针和头节点的区别" class="headerlink" title="1.5 头指针和头节点的区别"></a>1.5 头指针和头节点的区别</h2><p>⭐⭐</p>
<p>不管带不带头结点，头指针始终指向链表的第一个结点，而头结点是带头结点链表中的第一个结点，结点内通常不存储信息，它是为了方便做的一种处理。</p>
<p>引入头结点的<strong>优点</strong>：</p>
<ol>
<li><p>由于第一个数据节点的位置被存放在头结点的指针域中，因此在链表的第一个位置上的操作和在链表其他位置上的操作一致，无需进行特殊处理。</p>
</li>
<li><p>无论链表是否为空，其头指针都是指向头结点的非空指针，因此空表和非空表的处理也得到了统一。</p>
</li>
</ol>
<h2 id="1-6-判断链表是否有环"><a href="#1-6-判断链表是否有环" class="headerlink" title="&#x3D;&#x3D;1.6 判断链表是否有环&#x3D;&#x3D;"></a>&#x3D;&#x3D;<strong>1.6 判断链表是否有环</strong>&#x3D;&#x3D;</h2><p>&#x3D;&#x3D;<strong>（非常重要！）</strong>⭐⭐⭐⭐⭐⭐⭐&#x3D;&#x3D;</p>
<p>快慢指针：创建两个指针fast和slow，开始时两个指针都指向链表头head，在每一步操作中slow向前走一步，fast向前走两步，由于fast比slow移动的快，如果有环，两个指针就会相遇。</p>
<h2 id="1-7-循环队列"><a href="#1-7-循环队列" class="headerlink" title="&#x3D;&#x3D;1.7 循环队列&#x3D;&#x3D;"></a>&#x3D;&#x3D;1.7 循环队列&#x3D;&#x3D;</h2><p><strong>有一个循环队列Q，里面的编号是0到n-1，头尾指针分别是front，rear，现在求Q中元素的个数？</strong>⭐⭐</p>
<p>因为出队操作会导致队头的存储空间空闲，当rear指针指到maxsize的时候，其实是一种“假溢出”，这时候队列并没有满。为了解决这个问题，我们把存储队列元素的表从逻辑上视为一个环，称为循环队列。</p>
<p><strong>初始</strong>：front&#x3D;rear&#x3D;0；</p>
<p>出队：front &#x3D; (front+1)%maxsize 队头指针加一取模</p>
<p>入队：rear &#x3D; (raer+1)%maxsize  队尾指针加1取模</p>
<p><strong>队列长度</strong>&#x3D;(rear-front+maxsize)%maxsize</p>
<p><strong>如何区分循环队列是队空还是队满</strong>？</p>
<p>队满条件和队空条件都是rear &#x3D; front</p>
<p>因此为了区分这两个条件，可以采用的处理方式为：</p>
<ol>
<li>入队时少用一个存储单元，将差一个不满的条件视为队满，判断队空条件不变：队头和队尾指针在同一位置时，队空 rear &#x3D; front，判断队满条件变为：队头指针在队尾指针的下一位置时，队满，也就是（rear+1）%maxsize &#x3D; front </li>
<li>增设一个数据成员记录元素个数，队空条件为size &#x3D; 0；队满条件为size&#x3D;maxsize。</li>
</ol>
<h2 id="1-8-栈的应用"><a href="#1-8-栈的应用" class="headerlink" title="1.8 栈的应用"></a>1.8 栈的应用</h2><p><strong>（1）括号匹配</strong>：匹配过程为从左至右扫描表达式，每当遇到一个右括号时，就判断他和最后出现的那个左括号是否匹配，因此用栈存储被扫描到的左括号。每当扫描到右括号时，就让栈顶的左括号出栈。</p>
<p><strong>（2）后缀表达式的运算</strong>：运算过程是从左到右扫描表达式，每遇到一个运算符，就让运算符前面最近的两个操作数执行对应运算，合体成一个新的操作数。最后出现的操作数最先被运算，所以用栈来存储操作数。</p>
<p><strong>（3）中缀表达式转后缀表达式</strong>：栈中存储还未确定运算顺序的运算符。</p>
<p><strong>（4）递归</strong></p>
<p><strong>（5）进制转换</strong></p>
<p><strong>（6）深度优先搜索图</strong></p>
<p><strong>（7）前序遍历二叉树</strong></p>
<h2 id="1-9-队列的应用"><a href="#1-9-队列的应用" class="headerlink" title="1.9 队列的应用"></a>1.9 队列的应用</h2><p><strong>（1）层次遍历</strong></p>
<p><strong>（2）图的广度优先遍历</strong></p>
<p><strong>（3）打印机缓冲区</strong>（主机和外部设备速度不匹配）：先进入缓冲区排队的先打印</p>
<p><strong>（4）CPU资源竞争中的先来先服务策略</strong></p>
<h1 id="二、串"><a href="#二、串" class="headerlink" title="&#x3D;&#x3D;二、串&#x3D;&#x3D;"></a>&#x3D;&#x3D;二、串&#x3D;&#x3D;</h1><p>⭐⭐⭐</p>
<p><strong>朴素的匹配算法和KMP算法</strong></p>
<p><strong>朴素模式匹配算法</strong>：这个方法也称为暴力匹配。就是从源字符串开始搜索，若出现不能匹配，则从原搜索位置+1进行搜索。</p>
<p>主串长度：n，模式串长度m，最坏情况下共需对比n-m+1个子串，每个子串对比m个字符，时间复杂度为O(n*m)。</p>
<p><strong>KMP算法：</strong></p>
<p>KMP算法的<strong>思想</strong>是利用匹配失败后的信息，尽量减少子串与主串的匹配次数以达到快速匹配的目的。</p>
<p>算法实现的<strong>关键</strong>是求出next数组，next[j]表示：在子串的第j个字符与主串发生失配时，则跳到子串指针应该移动到的位置。</p>
<p><strong>求next数组的方法</strong>：在不匹配的位置前，画一个分界线，模式串一步步右移，直到分界线之前能匹配或者模式串完全跨过分界线为止。此时子串指针j指向哪儿，next数组的值就是多少。</p>
<p>next数组：next[i]表示在i位置之前的字符串中，同时也是模式串的后缀和前缀匹配的最大长度。</p>
<p>next[1]&#x3D;0 next[2]&#x3D;1</p>
<p><strong>匹配方法</strong>：匹配过程产生失配时，主串指针i不变，子串指针j退回到next[j]的位置并重新进行比较。</p>
<p><strong>时间复杂度</strong>：求next数组O(m)，模式匹配O(n),O(m+n)，主要优点是主串不回溯。</p>
<img src="https://pic1.zhimg.com/v2-f29d822e4faf22542875de6c73fe07d0_b.webp" alt="动图" style="zoom:50%;" />

<h1 id="三、树"><a href="#三、树" class="headerlink" title="三、树"></a>三、树</h1><h2 id="3-1-树的性质"><a href="#3-1-树的性质" class="headerlink" title="3.1 树的性质"></a>3.1 树的性质</h2><ul>
<li>树中的结点数等于所有结点的度数之和加1</li>
<li>度为m的树中第i层上至多有m^i-1^个结点(i&gt;&#x3D;1)</li>
<li>高度为h的m叉树至多有m^h^-1&#x2F;(m-1)个结点</li>
<li>具有n个结点的m叉树的最小高度为ceil[logm (n(m-1)+ 1)]。</li>
</ul>
<h2 id="3-2-哈夫曼树与哈夫曼编码"><a href="#3-2-哈夫曼树与哈夫曼编码" class="headerlink" title="3.2 哈夫曼树与哈夫曼编码"></a>3.2 哈夫曼树与哈夫曼编码</h2><p><strong>哈夫曼树</strong>：在含有n个带权叶结点的二叉树中，带权路径长度(WPL)最小的二叉树</p>
<p><strong>构造步骤</strong>：</p>
<ol>
<li>将这n个结点分别作为n棵仅含一个结点的二叉树，构成森林F。</li>
<li>构造一个新结点，从F中选取两棵根结点权值最小的树作为新结点的左、右子树，并且将新结点的权值置为左、右子树上根结点的权值之和。</li>
<li>从F中删除刚才选出的两棵树，同时将新得到的树加入F中。</li>
<li>重复步骤2和3，直至F中只剩下一棵树为止。</li>
</ol>
<p><strong>前缀编码</strong>：任何一个编码都不是其他编码的前缀（<strong>最左子串</strong>），哈夫曼编码就是<strong>最优前缀编码</strong></p>
<h2 id="3-3-树的分类及构造"><a href="#3-3-树的分类及构造" class="headerlink" title="&#x3D;&#x3D;3.3 树的分类及构造&#x3D;&#x3D;"></a>&#x3D;&#x3D;3.3 树的分类及构造&#x3D;&#x3D;</h2><p>⭐⭐⭐</p>
<ol>
<li>满二叉树</li>
<li>完全二叉树</li>
<li>二叉树排序</li>
<li>平衡二叉树：树上任一结点左子树和右子树深度之差不超过1.</li>
</ol>
<p>​	<strong>二叉树的顺序存储,二叉树的链式存储</strong></p>
<p><strong>如何由遍历序列构造一颗二叉树？&#x2F;已知先序序列和后序序列能否重现二叉树？</strong></p>
<p>不能，没有中序遍历序列的情况下是无法确定一颗二叉树的</p>
<p><strong>线索二叉树</strong></p>
<ul>
<li>在含有n个结点的二叉树中会有n+1个空指针，可以利用这些指针存放当前结点的前驱和后继指针，引入线索二叉树可以加快查找结点前驱和后继的速度。</li>
<li>要增加两个标志域标识指针域是指向左（右）孩子的还是指向前驱（后继）的。</li>
</ul>
<h2 id="3-4-树与森林"><a href="#3-4-树与森林" class="headerlink" title="3.4 树与森林"></a>3.4 树与森林</h2><ol>
<li><p><strong>树–&gt;二叉树</strong>：</p>
<ul>
<li><strong>加线</strong>。在所有的兄弟结点之间加一条线。</li>
<li><img src="https://pic3.zhimg.com/v2-1c30c5a655372ace13e8bf93fcbd84fa_b.webp" alt="动图" style="zoom: 33%;" /><img src="https://pic1.zhimg.com/v2-f08193a3566fe03c521b0fe829530320_b.webp" alt="动图" style="zoom:33%;" /><img src="https://pic3.zhimg.com/v2-2d10b1bb1f71ce902b07e5629a602e42_b.webp" alt="动图" style="zoom:33%;" /></li>
<li><strong>去线</strong>。树中的每个结点，只保留它与<strong>第一个孩子结点</strong>的连线，删除其他孩子结点之间的连线。</li>
<li>以树的根结点为轴心，将整个树调节一下（第一个孩子是结点的左孩子，兄弟转过来的孩子是结点的右孩子）</li>
<li>每个结点左指针指向它的第一个孩子，右指针指向它在树中相邻的右兄弟。</li>
</ul>
</li>
<li><p><strong>森林–&gt;二叉树</strong>：将森林中的每棵树都转换为二叉树，将第二棵二叉树作为第一棵的右子树，将第三棵二叉树作为第二棵二叉树的右兄弟，以此类推，直至只剩下唯一一棵二叉树。</p>
</li>
<li><p><strong>二叉树–&gt;森林</strong>：将根节点右链断开形成两棵二叉树，以此类推直至所有二叉树根节点都不存在右子树，最后将每棵二叉树都转换为树，得到森林。</p>
<p><img src="https://pic4.zhimg.com/v2-707f004bb9513f7332e25c1ab9e322e7_b.webp" alt="动图" style="zoom:33%;" /><img src="https://pic2.zhimg.com/v2-e2c977efa9bacdccae281913493df5ed_b.webp" alt="动图" style="zoom:33%;" /></p>
</li>
</ol>
<h2 id="3-5-并查集"><a href="#3-5-并查集" class="headerlink" title="3.5 并查集"></a>3.5 并查集</h2><p>每一个集合以森林中的一棵树表示，集合的并操作就转化成了合并两棵树（只需要改变第二棵树根节点的指向），集合的查操作就转化成了判断两个结点是否在同一棵树中。</p>
<h1 id="四、图"><a href="#四、图" class="headerlink" title="四、图"></a>四、图</h1><p>邻接矩阵适合稠密图 邻接表适合稀疏图</p>
<h2 id="4-1-最小生成树"><a href="#4-1-最小生成树" class="headerlink" title="&#x3D;&#x3D;4.1 最小生成树&#x3D;&#x3D;"></a>&#x3D;&#x3D;4.1 最小生成树&#x3D;&#x3D;</h2><p>图的所有生成树中具有边上的权值之和最小的树</p>
<ul>
<li>Prim算法的基本思想是<strong>选点</strong>：<ul>
<li><p>初始时从图中任取一顶点加入树T，之后选择一个与当前T中顶点集合距离最近的顶点，并将该顶点和相应的边加入T，每次操作T中的顶点数和边数都加1。</p>
</li>
<li><p>以此类推，直至图中所有的顶点都并入T，得到的T就是最小生成树。</p>
</li>
<li><p><strong>实现方法</strong>：两个数组，一个用来存储各顶点加入生成树的最小代价，一个用来存储各顶点是否已经加入了最小生成树。每次归并一个新顶点前，要遍历这两个数组找到还未加入树且权值最小的顶点；归并一个新顶点后要更新这两个数组。</p>
<p><strong>时间复杂度</strong>：此算法是双层循环，外层循环控制次数，一共要归并n个顶点，因此要循环n-1次，内层并列两个循环，每个循环负责遍历一个数组，因此此算法的时间复杂度为O(n^2^)，时间复杂度只与顶点有关，与边无关，适用于稠密图。</p>
</li>
</ul>
</li>
<li>Kruskal算法的基本思想是<strong>选边</strong>：<ul>
<li><p>初始时构造只有n个顶点但是没有边的非连通图，每个顶点自成一个连通分量，然后按边的权值升序枚举每条边</p>
<ul>
<li>若该边依附的顶点落在T中不同的连通分量上，则将此边加入T</li>
<li>否则舍弃此边而选择下一条权值最小的边</li>
</ul>
</li>
<li><p>以此类推，直至T中所有顶点都在一个连通分量上，得到的T就是最小生成树。</p>
</li>
<li><p><strong>实现方法</strong>：将图中边按照权值从小到大排列，然后从最小的边开始扫描，设置一个边的集合来记录，如果该边并入不构成回路的话，则将该边并入当前生成树。直到所有的边都检测完为止。</p>
<p><strong>时间复杂度</strong>：用堆来存放边的集合O(logE)，用并查集来判断2顶点是否连通（是否属于同一个集合），O(ElogE)</p>
<p>时间复杂度只与边有关，适合边稀疏顶点多的图</p>
</li>
</ul>
</li>
</ul>
<h2 id="4-2-最短路径"><a href="#4-2-最短路径" class="headerlink" title="&#x3D;&#x3D;4.2 最短路径&#x3D;&#x3D;"></a>&#x3D;&#x3D;4.2 最短路径&#x3D;&#x3D;</h2><ul>
<li><p>Dijkstra算法是求单源最短路径算法，用于求某一顶点到其他顶点的最短路径</p>
<ol>
<li><p>迪杰斯特拉算法的基本思想是逐步找到从起始节点到其他节点的最短路径，通过不断更新节点的距离信息来实现。算法维护一个距离数组，其中存储了从起始节点到每个节点的当前最短路径估计</p>
</li>
<li><p>算法的具体步骤如下：</p>
<ol>
<li>初始化距离数组，将起始节点的距离设为0，其他节点的距离设为无穷大。</li>
<li>从距离数组中选择当前距离最小的节点，标记为已访问。</li>
<li>遍历以该节点为起点的所有边，更新与这些边相连的节点的距离。如果通过当前节点可以获得更短的路径，就更新距离数组中的值。</li>
<li>重复步骤2和3，直到所有节点都被访问过或者目标节点的距离已经确定为最短路径。</li>
</ol>
<p>时间复杂度为O(n^2^) ，其中n为图中的顶点数。</p>
</li>
</ol>
</li>
<li><p>Floyd算法是求任意顶点之间的最短路径的算法，其边的权值可为负权。</p>
<ul>
<li>Floyd算法的基本思想是采用动态规划的方法，通过逐步迭代来更新节点之间的最短路径距离。</li>
<li>算法的核心是一个三重循环，第一层循环遍历各个节点作为中间节点，第二、三层循环遍历源点i和终点j，看是否能通过这个中间节点来获得更短的路径。</li>
</ul>
</li>
</ul>
<h2 id="4-3-拓扑排序"><a href="#4-3-拓扑排序" class="headerlink" title="4.3 拓扑排序"></a>4.3 拓扑排序</h2><p><strong>AOV网</strong>：以顶点为活动，边为活动间先后顺序关系的有向图</p>
<p>在AOV网中如果不存在回路，则可以把所有的活动排列成一个序列，称该序列为<strong>拓扑序列</strong></p>
<p>拓扑排序可以决定哪些子工程必须要先执行，哪些子工程要在某些工程完成后才能执行。</p>
<p>步骤：</p>
<ol>
<li>在有向图中任意选择一个没有前驱的节点输出</li>
<li>从图中删去该节点以及与它相连的边</li>
<li>重复步骤1、2，直到所有的顶点都输出或者当前图中不存在无前驱的顶点为止，后者代表该图是有环图</li>
</ol>
<p><strong>时间复杂度：</strong>由于删除每个顶点的同时还要删除以他为起点的边，因此每个顶点和每条边都会被遍历一次。采用邻接表存储方式：O(V+E)，采用邻接矩阵存储方式：O(V^2^)</p>
<h2 id="4-4-关键路径"><a href="#4-4-关键路径" class="headerlink" title="4.4 关键路径"></a>4.4 关键路径</h2><p><strong>AOE网</strong>：用顶点表示事件，边表示活动，边的权值表示两个活动持续的时间。</p>
<p>AOE网从源点到汇点的路径中，具有最大路径长度的路径为<strong>关键路径</strong>，关键路径上的活动称为<strong>关键活动</strong></p>
<p>步骤：</p>
<ol>
<li>按拓扑序依次求各个事件的最早发生时间ve</li>
<li>求各个事件的最晚开始时间vl</li>
<li>求各个活动的最早开始时间e</li>
<li>求各个活动的最晚开始时间l</li>
<li>求所有活动的时间余量，余量为0的活动为关键活动</li>
</ol>
<h2 id="4-5-图的基本概念"><a href="#4-5-图的基本概念" class="headerlink" title="4.5 图的基本概念"></a>4.5 图的基本概念</h2><ol>
<li><p><strong>完全图</strong>：在全图的任意两个顶点间都存在边</p>
</li>
<li><p><strong>连通图</strong>：图中任意两个顶点连通</p>
</li>
<li><p><strong>连通分量</strong>：无向图中的极大连通子图</p>
</li>
<li><p><strong>强连通</strong>：在有向图中，如果有一对顶点v和w，从v到w和从w到v都有路径，则称这两个顶点是强连通的</p>
</li>
<li><p><strong>强连通分量</strong>：极大强连通子图</p>
</li>
<li><p><strong>生成树</strong>：包含图中全部顶点的极小连通子图。</p>
</li>
</ol>
<p>⭐</p>
<ul>
<li><p><strong>邻接矩阵法</strong>（适用于存储稠密图）：用一个一维数组存储图中顶点信息，用一个二维数组存储图中边的信息,存储顶点之间邻接关系的二维数组称为邻接矩阵。空间复杂度O(n^2^)。</p>
</li>
<li><p><strong>邻接表法</strong>（适用于存储稀疏图）：图中顶点用一个一维数组存储，存储顶点数据和边表头指针。为每个顶点建立一个单链表，存储依附于此顶点的边，这个单链表就成为边表（对于有向图来说叫出边表）空间复杂度：无向图（每条边存储了两次）：O(V+2E) 有向图O(V+E)</p>
</li>
</ul>
<h2 id="4-6-DFS和BFS"><a href="#4-6-DFS和BFS" class="headerlink" title="&#x3D;&#x3D;4.6 DFS和BFS&#x3D;&#x3D;"></a>&#x3D;&#x3D;4.6 DFS和BFS&#x3D;&#x3D;</h2><p>⭐⭐⭐</p>
<ol>
<li><p><strong>广度优先搜索</strong></p>
<p><strong>时间复杂度</strong>&#x3D;访问顶点的时间+找邻接顶点的时间，采用邻接表存储方式：O(V+E)，采用邻接矩阵存储方式：O(V^2^)</p>
</li>
<li><p><strong>深度优先搜索</strong>：深度优先搜索类似于树的先序遍历，他的策略是尽可能深的搜索一个图。</p>
<p><strong>基本思想</strong>是：首先访问图中某一起始顶点v，然后由v出发，访问与v邻接但未被访问的顶点w1，再访问与W1邻接但未被访问的顶点w2…..重复上述过程，当不能再向下访问时，依次退回到最近被访问的结点，若他还有顶点未被访问过，则从该点开始继续上述的搜索过程，直至图中所有顶点都被访问过为止。</p>
<p><strong>时间复杂度</strong>&#x3D;访问顶点的时间+找邻接顶点的时间，用邻接表存储方式：O(V+E)，采用邻接矩阵存储方式：O(V^2^）</p>
</li>
</ol>
<h1 id="五、查找"><a href="#五、查找" class="headerlink" title="五、查找"></a>五、查找</h1><h2 id="5-1-顺序查找-分块查找"><a href="#5-1-顺序查找-分块查找" class="headerlink" title="5.1 顺序查找 分块查找"></a>5.1 顺序查找 分块查找</h2><p><strong>平均查找长度（ASL）</strong>所有查找过程中进行关键字的比较次数的平均值</p>
<p>按顺序查找<strong>优化思路</strong>：若每个元素被查的概率不等，则将被查概率大的放在靠前的位置。</p>
<p>按块查找(区间)索引结构</p>
<p>将查找表分为若干子块，块内元素无序，块间有序，即第一个块中的最大关键字小于第二个块中所有记录的关键字，以此类推。再建立一个索引表索引，表中的每个元素含有各块的最大关键字和各块第一个元素的地址，索引表按关键字有序排列。</p>
<p>查找过程分为两步，第一步是在索引表中确定待查记录所在的块，可以顺序或折半查找索引表；第二步是在块内顺序查找。</p>
<h2 id="5-2-折半查找"><a href="#5-2-折半查找" class="headerlink" title="5.2 折半查找"></a>5.2 折半查找</h2><ul>
<li>首先将给定值key与有序表的中间位置的元素比较；<ul>
<li>若相等，则查找成功，返回该元素的存储位置；</li>
<li>若不等，则所需查找的元素只需继续查找中间元素的前半部分或后半部分；<ul>
<li>然后在缩小的范围内继续进行同样的查找，直到找到为止，或确定表中没有所需要查找的元素，则查找不成功，返回查找失败的信息。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>时间复杂度O(log_n)，要求查找表为顺序存储结构并且有序；</p>
<h2 id="5-3-二叉排序树"><a href="#5-3-二叉排序树" class="headerlink" title="5.3 二叉排序树"></a>5.3 二叉排序树</h2><ul>
<li>若该树有左子树，则其左子树的所有节点值小于根的值；</li>
<li>若该树有右子树，则其右子树的所有节点值均大于根的值；</li>
<li>其左右子树也分别为二叉排序树</li>
</ul>
<p><strong>构造</strong>：从一棵空树出发，依次输入元素，将他们插入二叉排序树中的合适位置，使其符合二叉排序树定义。</p>
<p><strong>删除</strong>：如果删除节点是叶节点，则直接删除；如果是非叶节点且只有一棵子树，则让这棵子树成为被删除结点父节点的子树；若是非叶节点且有左右两棵子树，则用被删除节点的直接前驱或者直接后继(中序遍历)替代。</p>
<p><strong>平均查找长度</strong>：二叉排序树的平均查找长度主要取决于树的高度。最好情况：二叉排序树的平衡的(左右子树高度之差不超过1)，O(log2 n)；最坏情况：二叉排序树是一个只有左（右）孩子的单支树，O(n)</p>
<h2 id="5-4-平衡二叉树："><a href="#5-4-平衡二叉树：" class="headerlink" title="5.4 平衡二叉树："></a>5.4 平衡二叉树：</h2><p>又称为<strong>AVL树</strong>，它具有如下特点：</p>
<ul>
<li>他的左右子树高度差的绝对值不能大于1，且他的左右子树也都是平衡二叉树。</li>
</ul>
<p>特点：执行插入和删除操作后，非常频繁地调整全树整体拓扑结构</p>
<p><strong>插入</strong>：每当在二叉排序树中插入（删除）一个结点时，首先检查其插入路径上的结点是否因为此次操作而导致了不平衡。如果导致了不平衡，则先找到插入路径上离插入节点最近的平衡因子的绝对值大于1的结点A，再对以A为根的子树（最小平衡子树）进行旋转。</p>
<ul>
<li><p>如果由于在结点A的左孩子的左子树上插入新节点而导致失去平衡，则通过一次右单旋转调整。</p>
</li>
<li><p>如果由于在结点A的右孩子的右子树上插入新节点而导致失去平衡，则通过一次左单旋转调整。</p>
</li>
<li><p>如果由于在结点A的左孩子的右子树上插入新结点而导致失去平衡，则先通过一次左旋再通过一次右旋调整。</p>
</li>
<li><p>如果由于在结点A的右孩子的左子树上插入新结点而导致失去平衡，则先通过一次右旋再通过一次左旋调整。</p>
</li>
</ul>
<p><strong>平均查找长度：</strong>含有n个节点的平衡二叉树的最大深度为O(log2 n)，因此平衡二叉树的平均查找长度为O(log2 n)</p>
<p>左旋：将右子树的左子树链接到父亲节点的右孩子结点</p>
<h2 id="5-5-红黑树"><a href="#5-5-红黑树" class="headerlink" title="&#x3D;&#x3D;5.5 红黑树&#x3D;&#x3D;"></a>&#x3D;&#x3D;5.5 红黑树&#x3D;&#x3D;</h2><p>⭐⭐⭐</p>
<p>BST 插入 删除 修改都是O(n)</p>
<p>红黑树和二叉排序树都是O(log2n)</p>
<p>一棵红黑树是满足如下红黑性质的二叉排序树：</p>
<ol>
<li>每个结点或是红色，或是黑色的。</li>
<li>根结点是黑色的。</li>
<li>叶结点都是黑色的。</li>
<li>不存在两个相邻的红结点</li>
<li>对每个结点，从该结点到任意一个叶结点的简单路径上，所含黑结点的数量相同。</li>
</ol>
<ul>
<li>结论1：从根到叶结点的最长路径不大于最短路径的2倍。</li>
<li>结论2：有n个内部结点的红黑树的高度h≤2log_2(n+ 1)。</li>
</ul>
<p>红黑树的“适度平衡”，由 AVL树的“高度平衡”，降低到“任意一个结点左右子树的高度，相差不超过2倍”，也降低了动态操作时调整的频率。放宽条件。对于红黑树来说，插入删除操作很多时候不会破坏其红黑特性，无需频繁调整树的形态，红黑树常数级时间内能完成修改</p>
<p>C++中的map和set、Java中的TreeMap和TreeSet都是用红黑树实现的。</p>
<p><strong>插入</strong>：先进行查找操作，确定插入位置并插入。如果新结点是根，则染为黑色，若新节点非根，则染为红色。如果插入后不满足红黑树定义，则需要调整使其重新满足红黑树定义。</p>
<h2 id="5-6-B树"><a href="#5-6-B树" class="headerlink" title="5.6 B树"></a>5.6 B树</h2><p><img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20240330205312381.png" alt="image-20240330205312381"></p>
<p>关键字顺序类比BST</p>
<p><strong>查找</strong>：1.在B树里找结点 2.在结点里找关键字</p>
<p><strong>插入</strong>：</p>
<ol>
<li>首先进行查找操作，找出插入该关键字的最低层中的某个非叶节点。</li>
<li>进行插入操作，如果插入后被插入节点的关键字个数超过m-1，则需要对节点进行分裂。</li>
<li>分裂方法是：从中间位置将关键字一分为二，左部分放在原结点中，右部分放在新结点中，中间位置的关键字插入原节点的父结点中。若此时导致父节点关键字个数也超过上限，则继续分裂。</li>
</ol>
<p><strong>删除</strong>：如果被删除的关键字k不在终端结点中时，可以用k的前驱或者后继来替代k。如果被删除的关键字在终端结点，则有2种情况：如果被删除关键字所在节点删除前的关键字个数&gt;&#x3D;m&#x2F;2，则直接删除。反之需要重新调整结点，向兄弟结点借一个关键字或者与兄弟节点和双亲结点中的关键字进行合并。</p>
<p>用作数据库索引</p>
<h2 id="5-7-B-树"><a href="#5-7-B-树" class="headerlink" title="&#x3D;&#x3D;5.7 B+树&#x3D;&#x3D;"></a>&#x3D;&#x3D;5.7 B+树&#x3D;&#x3D;</h2><p>⭐⭐⭐</p>
<p>与分块查找类似</p>
<ul>
<li>具有n个关键字的结点只含有n棵子树，即每个关键字对应一棵子树；</li>
<li>叶结点包含了全部关键字，非叶结点中出现的关键字也会出现在叶结点中；</li>
<li>叶结点包含信息，所有非叶结点仅起索引作用，非叶结点中的每个索引项只含有对应子树的最大关键字和指向该子树的指针，不含有该关键字对应记录的存储地址；</li>
</ul>
<img src="https://luhaoblog.oss-cn-hangzhou.aliyuncs.com/img1/image-20240330210408289.png" alt="image-20240330210408289" style="zoom:67%;" />

<p><strong>B数和B+树的区别</strong></p>
<ol>
<li><p>B+树具有n个关键字的结点只含有n棵子树，即每个关键字对应一棵子树。而在B树中，具有n个关键字的结点含有n+1棵子树。</p>
</li>
<li><p>B+树叶结点包含了全部关键字，即在非叶结点中出现的关键字也会出现在叶结点中。而在B树中，叶结点包含的关键字与其他结点包含的关键字是不重复的。</p>
</li>
<li><p>B+树非叶节点仅起索引作用，叶结点包含关键字对应记录的存储地址.而B树的结点中都包含关键字对应记录的存储地址。</p>
</li>
</ol>
<p><strong>为什么有了B树还要提出B+树</strong></p>
<ul>
<li><strong>结构差异</strong>：B树的关键字分布在所有节点中，而B+树的所有关键字都存储在叶子节点中。</li>
<li><strong>查询效率</strong>：对于单点查询，B树和B+树的效率相近。但对于范围查询，B+树由于叶子节点间的链接，效率更高。</li>
<li><strong>空间利用率</strong>：B+树由于非叶子节点不存储数据，空间利用率更高，能够存储更多的关键字。</li>
</ul>
<h2 id="5-8-哈希表"><a href="#5-8-哈希表" class="headerlink" title="&#x3D;&#x3D;5.8 哈希表&#x3D;&#x3D;"></a>&#x3D;&#x3D;5.8 哈希表&#x3D;&#x3D;</h2><p>⭐⭐⭐⭐</p>
<p><strong>哈希表</strong>又称为散列表，通过把关键码的值直接映射到表中的一个位置，以加快查找速度。</p>
<p><strong>构造方法：</strong></p>
<ol>
<li><p>直接定址法：直接取某个线性函数值作为散列地址</p>
</li>
<li><p>除留取余法：取一个最接近散列表表长的质数p，散列函数为key%p</p>
</li>
<li><p>数字分析法：选取数码分布较为均匀的若干位作为散列地址</p>
</li>
<li><p>平方取中法：取平方值的中间几位作为散列地址。</p>
</li>
</ol>
<p><strong>处理冲突的方法：</strong></p>
<ol>
<li><strong>开放定址法</strong>：指可存放新表项的空闲地址既向它的同义词表项开放，又向它的非同义词表项开放。<ul>
<li><strong>线性探测法</strong>：冲突发生时，顺序查看表中下一个单元，直到找出一个空闲单元，缺点：大量元素在相邻的散列地址上堆积起来，降低了查找效率。</li>
<li><strong>平方探测法</strong>：增量序列为0^2^,1^2^,-1^2^,2^2^…以此类推。优点：避免出现堆积问题，缺点：不能探测到散列表上的所有单元。</li>
<li><strong>双散列法</strong>：需要使用两个散列函数，当通过第一个散列函数得到的地址发生冲突时，则利用第二个散列函数计算该关键字的地址增量。</li>
<li><strong>伪随机序列法</strong>：增量为随机数。</li>
</ul>
</li>
<li><strong>拉链法</strong>：不同的关键字会通过散列函数映射到同一地址时，为了避免非同义词发生冲突，可以把所有的同义词存储在一个线性链表中，这个线性链表由其散列地址唯一标识。</li>
</ol>
<h1 id="六、排序"><a href="#六、排序" class="headerlink" title="&#x3D;&#x3D;六、排序&#x3D;&#x3D;"></a>&#x3D;&#x3D;六、排序&#x3D;&#x3D;</h1><p><strong>基本思想是什么？时间复杂度？是否稳定？给一个例子，问冒泡和快速排序在最坏的情况下比较几次？</strong>★★★★★★</p>
<p><img src="https://cdn.acwing.com/media/article/image/2020/12/05/41924_1a3c844236-sort.png" alt="sort.png"></p>
<h2 id="6-1-插入排序"><a href="#6-1-插入排序" class="headerlink" title="6.1 插入排序"></a>6.1 插入排序</h2><blockquote>
<p>将序列分为有序部分和无序部分，从无序部分依次选择元素与有序部分比较找到合适的位置，将原来的元素往后移，将元素插入到相应位置上。</p>
</blockquote>
<h2 id="6-2-希尔排序"><a href="#6-2-希尔排序" class="headerlink" title="6.2 希尔排序"></a>6.2 希尔排序</h2><blockquote>
<p>把元素按下标的一定增量d进行分组，对每组使用直接插入排序。随着增量逐渐减少，当增量减至1时，整个文件恰被分为一组，算法终止。</p>
</blockquote>
<p>优点是：让关键字值小的元素能够很快移动到前面，且序列基本有序时进行直接插入排序时间效率会提升很多</p>
<h2 id="6-3-简单选择排序"><a href="#6-3-简单选择排序" class="headerlink" title="6.3 简单选择排序"></a>6.3 简单选择排序</h2><blockquote>
<p>将序列分为有序部分和无序部分，每经过一趟就在无序部分找到一个最小值然后与无序部分的第一个元素交换位置。</p>
</blockquote>
<p>优点是：实现简单，缺点是：每一趟只能确定一个元素的位置，时间效率低。</p>
<h2 id="6-4-堆排序"><a href="#6-4-堆排序" class="headerlink" title="6.4 堆排序"></a>6.4 堆排序</h2><blockquote>
<p>堆是特殊的完全二叉树，树中任意节点均大于或小于其左右孩子</p>
</blockquote>
<blockquote>
<p>将无序序列构建成一个堆，根据升序和降序需求选择大根堆或者小根堆，将堆顶元素与末尾元素交换，通过up和down操作重新调整堆，反复执行调整交换步骤，直到整个序列有序。</p>
</blockquote>
<h2 id="6-5-冒泡排序"><a href="#6-5-冒泡排序" class="headerlink" title="6.5 冒泡排序"></a>6.5 冒泡排序</h2><blockquote>
<p>基本思路为：每一趟都将相邻两元素进行比较，出现逆序情况则进行交换。</p>
</blockquote>
<h2 id="6-6-快速排序"><a href="#6-6-快速排序" class="headerlink" title="6.6 快速排序"></a>6.6 快速排序</h2><blockquote>
<p>基本思路为：在序列中任意选择一个元素作为中心，比它大的元素一律向后移动，比它小的元素一律向前移动，形成左右两个子序列，再把子序列按上述操作进行调整，直到所有的子序列中都只有一个元素时序列即为有序。</p>
<p>每次由一个枢轴将待排序序列分成左右两部分，这个过程类似于构造一棵二叉树，最小高度为log2 n,最大高度为n</p>
</blockquote>
<p>优点是：每一趟能确定一个元素，时间效率较高。</p>
<h2 id="6-7-基数排序"><a href="#6-7-基数排序" class="headerlink" title="6.7 基数排序"></a>6.7 基数排序</h2><blockquote>
<p>从最低位开始，对所有元素按照当前位的数值进行排序，直到对最高位进行排序。</p>
<p>设置r个空队列，按照各个关键字位权重递增的次序，对d个关键字位分别进行分配和收集操作。</p>
<p>分配：顺序扫描各个元素，根据当前处理的关键字位将元素插入相应队列。</p>
<p>收集：把各个队列中的结点依次出队并连接。</p>
</blockquote>
<h2 id="6-8-归并排序"><a href="#6-8-归并排序" class="headerlink" title="6.8 归并排序"></a>6.8 归并排序</h2><blockquote>
<p>将待排序表中的n个记录视为n个有序子表，每个子表长度为1。然后两两归并，得到n&#x2F;2个长度为2的有序表，继续两两归并，直至归并成一个长度为n的有序表为止。</p>
<p>初始化两个数组A和B，将两个要进行归并操作的有序表复制到数组B的相邻位置，每次从B中的两个有序表分别取出一个关键字进行比较，将大的存入A，重复上述操作直至B空。</p>
</blockquote>
<h1 id="七、其它"><a href="#七、其它" class="headerlink" title="&#x3D;&#x3D;七、其它&#x3D;&#x3D;"></a>&#x3D;&#x3D;七、其它&#x3D;&#x3D;</h1><h2 id="7-1-时间复杂度"><a href="#7-1-时间复杂度" class="headerlink" title="7.1 时间复杂度"></a>7.1 时间复杂度</h2><p>一个语句的频度是指该语句在算法中被重复执行的次数。算法中所有语句的频度之和记为T(n), 它是该算法问题规模n的函数，时间复杂度主要分析T(n) 的数量级。T(n) &#x3D; O(f(n))</p>
<p>时间复杂度描述了算法所需的时间资源随着输入规模的增加而增加的速度。它是一种用来描述算法运行时间增长速度的渐近符号。大O记法表示算法时间复杂度的上界，即在最坏情况下，算法的运行时间不会超过某个常数乘以某个函数。</p>
<blockquote>
<p>取f(n) 中随n增长最快的项，将其系数置为1 作为时间复杂度的度量。例如， f(n) &#x3D; an^3^ + bn^2^+ cn 的时向复杂度为O(n^3^)</p>
</blockquote>
<h2 id="7-2-空间复杂度"><a href="#7-2-空间复杂度" class="headerlink" title="7.2 空间复杂度"></a>7.2 空间复杂度</h2><p>空间复杂度描述了算法所需的存储空间与输入规模之间的关系。空间复杂度可以衡量算法对内存的利用情况</p>
<p>一个程序在执行时除需要存储空间来存放本身所用的指令、常数、变量和输入数据外，还需要一些对数据进行操作的工作单元和存储一些为实现计算所需信息的辅助空间。</p>
<h2 id="7-3-算法"><a href="#7-3-算法" class="headerlink" title="7.3 算法"></a>7.3 算法</h2><p>算法是指解决特定问题或执行特定任务的一系列步骤或规则。算法是独立于编程语言的，它描述了解决问题的方法，而不是具体的实现方式。算法是一种用来描述解决问题的有限步骤集合。</p>
<h2 id="7-4-数据结构三要素"><a href="#7-4-数据结构三要素" class="headerlink" title="7.4 数据结构三要素"></a>7.4 数据结构三要素</h2><p><strong>数据结构三要素是什么？逻辑结构包括什么？存储结构包括什么？</strong></p>
<p>逻辑结构、存储结构、数据运算</p>
<p>逻辑结构包括线性结构和非线性结构</p>
<p>线性结构包括线性表、栈、队列，非线性结构包括树、图集合</p>
<p>存储结构包括顺序存储、链式存储、索引存储和散列存储</p>
<h2 id="7-5-循环与递归"><a href="#7-5-循环与递归" class="headerlink" title="7.5 循环与递归"></a>7.5 循环与递归</h2><p>循环和递归两者是可以互换的，不能决定性的说循环的效率比递归高。</p>
<p>递归的优点是：代码简洁清晰，容易检查正确性；缺点是：当递归调用的次数较多时，要增加额外的堆栈处理，有可能产生堆栈溢出的情况，对执行效率有一定的影响。</p>
<p>循环的优点是：结构简单，速度快；缺点是：它并不能解决全部问题，有的问题适合于用递归来解决不适合用循环。</p>
<h2 id="7-6-贪心动态规划及分治法"><a href="#7-6-贪心动态规划及分治法" class="headerlink" title="7.6 贪心动态规划及分治法"></a>7.6 贪心动态规划及分治法</h2><p><strong>贪心算法</strong>顾名思义就是做出在当前看来是最好的结果，它不从整体上加以考虑，也就是局部最优解。贪心算法从上往下，从顶部一步一步最优，得到最后的结果，它不能保证全局最优解。</p>
<p><strong>动态规划</strong>是把问题分解成子问题，这些子问题可能有重复，可以记录下前面子问题的结果防止重复计算。动态规划解决子问题，前一个子问题的解对后一个子问题产生一定的影响。动态规划是从下到上，一步一步找到全局最优解。（各子问题重叠）</p>
<p><strong>分治法</strong>：将原问题划分成n个规模较小而结构与原问题相似的子问题；</p>
<p><strong>递归</strong>地解决这些子问题，然后<strong>再合并</strong>其结果，就得到原问题的解。（各子问题独立）</p>
<p>分治模式在每一层递归上都有三个步骤</p>
<p>分解：将原问题分解成一系列子问题；</p>
<p>解决：递归地解各个子问题。若子问题足够小，则直接求解；</p>
<p>合并：将子问题的结果合并成原问题的解。</p>
<p>例如归并排序。</p>
]]></content>
      <categories>
        <category>保研复习</category>
      </categories>
      <tags>
        <tag>保研面试题</tag>
      </tags>
  </entry>
</search>
